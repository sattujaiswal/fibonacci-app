name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Print Docker Hub Username
        run: echo ${{ secrets.DOCKERHUB_USERNAME }}

      - name: Print Docker Hub Token Length
        run: echo ${{ secrets.DOCKERHUB_TOKEN }} | wc -c

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and tag Docker image
        run: |
          docker build . --file Dockerfile --tag sattu2024/fibonacci-app:latest

      - name: Push Docker image to Docker Hub
        run: |
          docker push sattu2024/fibonacci-app:latest